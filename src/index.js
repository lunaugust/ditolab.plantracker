/**
 * Entry point — re-exports App as the default export
 * to maintain backward compatibility with the original
 * single-file structure.
 */
import "./styles/global.css";

export { default } from "./App";
